<!--components/headTitle/cmp.wxml-->
<scroll-view scroll-x class="container"  scroll-into-view="{{activeId}}" scroll-with-animation="true">
    <text class="type {{index === magazineIndex ? 'active' : ''}}" 
            wx:for="{{magazineTypeArr}}" wx:key="*this" id="{{'magazine'+index}}"
            data-index="{{index}}" catch:tap="onTap">{{item}}</text>  
</scroll-view>
